/**
 * Provides configuration IO implementations for reading and writing edn files.
 *
 * Edn is a subset of Clojure, so now you can use LISP for everything, even configuration.
 */
package com.oracle.labs.mlrg.olcut.config.edn;